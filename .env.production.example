# ===========================================
# SALES MENTOR - PRODUCTION ENVIRONMENT
# ===========================================
# Copie este arquivo para .env.production no servidor
# e preencha com os valores reais

# Database (PostgreSQL)
DATABASE_URL=postgres://USER:PASSWORD@localhost:5432/salesmentor

# OpenAI API
OPENAI_API_KEY=sk-proj-YOUR_OPENAI_API_KEY_HERE

# JWT Secret (MUDE ESTE VALOR!)
# Gere um secret forte: openssl rand -hex 32
INSIGHTS_JWT_SECRET=your_production_secret_change_me_MUST_BE_STRONG

# Node Environment
NODE_ENV=production

# Server Port
PORT=8080

# CORS Allowed Origins (URLs da extensão Chrome)
# Formato: chrome-extension://EXTENSION_ID
# Você obterá o EXTENSION_ID depois de instalar a extensão
# Deixe vazio por enquanto, será configurado depois
ALLOWED_ORIGINS=chrome-extension://YOUR_EXTENSION_ID_HERE
